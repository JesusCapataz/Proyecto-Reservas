# Desactivar Flyway en los tests para que no busque scripts
spring.flyway.enabled=false

# Decirle a Hibernate que cree las tablas automáticamente
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true